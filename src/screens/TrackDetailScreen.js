import React from "react";
import { Text, View, StyleSheet } from "react-native";

const TrackDetail = () => {
  return <Text style={{ fontSize: 40 }}>TrackDetail</Text>;
};

const styles = StyleSheet.create({});

export default TrackDetail;
