import React from "react";
import { Text, View, StyleSheet } from "react-native";

const TrackList = () => {
  return <Text style={{ fontSize: 40 }}>TrackList</Text>;
};

const styles = StyleSheet.create({});

export default TrackList;
